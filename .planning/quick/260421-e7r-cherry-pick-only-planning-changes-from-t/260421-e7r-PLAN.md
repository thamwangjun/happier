---
phase: quick-260421-e7r
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/
autonomous: true
requirements:
  - QUICK-260421-e7r
must_haves:
  truths:
    - ".planning/ directory on workspace/thamw-request-resilience matches thamw-dev"
    - "No non-.planning/ changes are introduced from thamw-dev"
    - "Current branch working tree is clean after the commit"
  artifacts:
    - path: ".planning/STATE.md"
      provides: "Planning state from thamw-dev"
    - path: ".planning/ROADMAP.md"
      provides: "Roadmap from thamw-dev"
    - path: ".planning/config.json"
      provides: "Planning config from thamw-dev"
  key_links:
    - from: "thamw-dev:.planning/"
      to: "workspace/thamw-request-resilience:.planning/"
      via: "git checkout thamw-dev -- .planning/"
      pattern: "git checkout"
---

<objective>
Copy the full `.planning/` directory state from `thamw-dev` into the current branch (`workspace/thamw-request-resilience`) without bringing in any non-`.planning/` code changes.

Purpose: The current branch was branched from an earlier point and is missing all planning history (STATE.md, ROADMAP.md, phase plans, summaries, codebase maps, etc.) that exists on `thamw-dev`. Bringing this over aligns the planning context without polluting the feature branch with unrelated code commits.

Output: A single commit on `workspace/thamw-request-resilience` that adds/updates all `.planning/` files to match `thamw-dev`, with no changes outside `.planning/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
Current branch: workspace/thamw-request-resilience
Source branch: thamw-dev

The current branch has only .planning/quick/ locally. thamw-dev has 89 .planning/ files
covering the full planning history (STATE.md, ROADMAP.md, RETROSPECTIVE.md, PROJECT.md,
codebase maps, phase plans, summaries, milestones archive, quick tasks, and config.json).

The safe approach is a path-scoped git checkout (not cherry-pick) because:
- Multiple commits on thamw-dev touched .planning/ interleaved with code commits
- Cherry-picking individual commits risks conflicts and brings in non-.planning/ hunks
- `git checkout thamw-dev -- .planning/` takes the current tree state of that path,
  exactly like copying the directory — clean, no history entanglement
</context>

<tasks>

<task type="auto">
  <name>Task 1: Copy .planning/ from thamw-dev and commit</name>
  <files>.planning/ (all files)</files>
  <action>
Run the following commands in sequence:

1. Bring the full .planning/ tree from thamw-dev into the working tree:
   ```
   git checkout thamw-dev -- .planning/
   ```
   This stages all changes under .planning/ as modifications/additions relative to
   the current branch. It does NOT touch any files outside .planning/.

2. Verify the staged changes are all under .planning/:
   ```
   git diff --cached --name-only
   ```
   Every line must start with `.planning/`. If any line does NOT start with `.planning/`,
   stop immediately and do not commit — report the unexpected file.

3. If all staged files are under .planning/, commit:
   ```
   git commit -m "chore(planning): bring .planning/ state from thamw-dev"
   ```

4. Confirm the working tree is clean:
   ```
   git status
   ```
  </action>
  <verify>
    <automated>git diff --cached --name-only | grep -v "^\.planning/" | wc -l</automated>
    Output of verify command must be `0` (zero files staged outside .planning/).
    After commit: `git status` shows "nothing to commit, working tree clean".
  </verify>
  <done>
    - All .planning/ files match thamw-dev (git diff thamw-dev -- .planning/ shows no output)
    - Zero non-.planning/ files were staged or committed
    - git log shows the new commit as the HEAD with message "chore(planning): bring .planning/ state from thamw-dev"
    - Working tree is clean
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| thamw-dev branch | Source of truth for .planning/ content — trusted (same repo, same author) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-e7r-01 | Tampering | git checkout scope | mitigate | Verify staged files with `git diff --cached --name-only` before committing — abort if any file is outside .planning/ |
| T-e7r-02 | Denial | Merge conflict | accept | thamw-dev .planning/ and current branch .planning/ are disjoint (current has only quick/); no conflict expected |
</threat_model>

<verification>
After the commit:

```bash
# No diff between thamw-dev .planning/ and current branch .planning/
git diff thamw-dev HEAD -- .planning/

# Expected: no output (identical .planning/ trees)
```

```bash
# No non-.planning/ changes introduced
git diff thamw-dev~1 HEAD --name-only | grep -v "^\.planning/"

# Expected: only the new PLAN.md in .planning/quick/ shows up (if anything)
```
</verification>

<success_criteria>
- `git diff thamw-dev HEAD -- .planning/` produces no output (trees are identical)
- `git log --oneline -1` shows the planning sync commit
- `git status` is clean
- No code files (outside .planning/) were modified on this branch
</success_criteria>

<output>
After completion, create `.planning/quick/260421-e7r-cherry-pick-only-planning-changes-from-t/260421-e7r-SUMMARY.md` with what was done, files affected count, and confirmation that no non-.planning/ files were touched.
</output>
