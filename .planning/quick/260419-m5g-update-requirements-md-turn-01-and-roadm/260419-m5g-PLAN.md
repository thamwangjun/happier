---
phase: quick-260419-m5g
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [TURN-01, TURN-04]
must_haves:
  truths:
    - "REQUIREMENTS.md TURN-01 describes the two-function split (finalizeSubagentTurn) instead of an isSubagent flag"
    - "REQUIREMENTS.md TURN-04 is updated to reflect that task_notification now calls finalizeSubagentTurn() directly, not passes a flag"
    - "ROADMAP.md Phase 4 SC-1 no longer references finalizeCurrentTurn({ isSubagent: true })"
    - "ROADMAP.md Phase 5 goal, SC-1, and SC-3 are revised to target end-to-end behavioral verification (TURN-06) instead of call-site flag wiring"
    - "All changes are consistent with the actual codebase implementation documented in 04-VERIFICATION.md"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated TURN-01 and TURN-04 requirements reflecting two-function-split design"
    - path: ".planning/ROADMAP.md"
      provides: "Updated Phase 4 SC-1 and Phase 5 goal/SCs reflecting the implemented design"
  key_links:
    - from: "REQUIREMENTS.md TURN-01"
      to: "claudeRemoteAgentSdk.ts line 1194"
      via: "finalizeSubagentTurn() closure"
      pattern: "finalizeSubagentTurn"
    - from: "ROADMAP.md Phase 5"
      to: "REQUIREMENTS.md TURN-06"
      via: "end-to-end ready-event verification"
      pattern: "TURN-06"
---

<objective>
Update REQUIREMENTS.md and ROADMAP.md to reflect the two-function-split design that Phase 4 actually implemented, closing the documentation gaps identified in 04-VERIFICATION.md.

Purpose: Phase 4 implemented `finalizeSubagentTurn()` as a separate closure (Codex-style) rather than adding `isSubagent?: boolean` to `finalizeCurrentTurn`. REQUIREMENTS.md TURN-01 and ROADMAP Phase 4 SC-1 still describe the original flag approach. Phase 5 goal and SCs are predicated on wiring isSubagent flags that no longer exist. These planning artifacts must match the codebase before Phase 5 can be planned.

Output: Updated REQUIREMENTS.md and ROADMAP.md that accurately describe what was built and what Phase 5 must verify.
</objective>

<execution_context>
@/home/thamw/.claude/get-shit-done/workflows/execute-plan.md
@/home/thamw/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-restructure-finalizecurrentturn/04-VERIFICATION.md

<interfaces>
<!-- Implemented design from 04-VERIFICATION.md (evidence from codebase) -->

From apps/cli/src/backends/claude/remote/claudeRemoteAgentSdk.ts:
- Line 109:  onSubagentFlush?: () => Promise<void>   (in opts type)
- Line 1169: const finalizeCurrentTurn = async (params?: { completionEvent?: string })  — NO isSubagent field
- Line 1194: const finalizeSubagentTurn = async () => { ... }  — separate closure, Phase A only
- Line 1564: task_notification handler calls await finalizeSubagentTurn()  (not finalizeCurrentTurn)
- Lines 1582, 1631, 1635: result handlers call await finalizeCurrentTurn(...)

From apps/cli/src/backends/claude/claudeRemoteLauncher.ts:
- Lines 992-995: onReady lambda — unchanged, calls messageQueue.flush() then readyHandler()
- Lines 996-998: onSubagentFlush lambda — calls messageQueue.flush() only, no readyHandler

Requirements satisfied by Phase 4 (from VERIFICATION.md):
- TURN-02: SATISFIED — both functions run Phase A bookkeeping
- TURN-03: SATISFIED — only finalizeCurrentTurn runs Phase B
- TURN-05: SATISFIED — messageQueue.flush() still unconditional in onReady
- TEST-01/02/03: SATISFIED — 4 tests, all GREEN

Requirements NOT satisfied as written:
- TURN-01: BLOCKED — finalizeCurrentTurn has no isSubagent param; two-function split used instead
- TURN-04: NOT COVERED — task_notification calls finalizeSubagentTurn() directly, no flag passed
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update REQUIREMENTS.md — revise TURN-01 and TURN-04 for two-function split</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
Rewrite TURN-01 and TURN-04 to reflect the implemented two-function-split design. Leave all other requirements (TURN-02, TURN-03, TURN-05, TURN-06, TEST-01/02/03) unchanged.

TURN-01 rewrite — replace:
  "finalizeCurrentTurn() in claudeRemoteAgentSdk.ts accepts isSubagent?: boolean in its params bag (backward-compatible with all existing call sites)"
with:
  "claudeRemoteAgentSdk.ts exposes two distinct closures for turn completion: finalizeCurrentTurn() for the parent path (all Phase A + Phase B logic, params?: { completionEvent?: string }) and finalizeSubagentTurn() for the subagent path (Phase A bookkeeping only). All existing call sites of finalizeCurrentTurn() remain unchanged."

TURN-04 rewrite — replace:
  "task_notification call site passes { isSubagent: true }; result and compact call sites pass {} (falsy default, treated as parent)"
with:
  "The task_notification handler calls finalizeSubagentTurn() directly. The result and compact call sites call finalizeCurrentTurn() (with optional completionEvent). No isSubagent flag is passed at any call site."

Also update the Traceability table: mark TURN-01 as "Phase 4 | Done" and TURN-04 as "Phase 4 | Done" (both were resolved by the two-function split in Phase 4, not deferred to Phase 5).

Update the Future Requirements section: move the "Dedicated subagent handler function (Codex-style finalizeSyntheticSubagentThread)" note to "Out of Scope" or remove it, since that pattern is now implemented.
  </action>
  <verify>grep -n "finalizeSubagentTurn" /home/thamw/development/happier/happier/.planning/REQUIREMENTS.md | head -5</verify>
  <done>REQUIREMENTS.md TURN-01 and TURN-04 describe the two-function-split design with no mention of isSubagent? flag. Traceability table shows both as Phase 4 Done.</done>
</task>

<task type="auto">
  <name>Task 2: Update ROADMAP.md — fix Phase 4 SC-1 and revise Phase 5 for two-function-split</name>
  <files>.planning/ROADMAP.md</files>
  <action>
Make two targeted updates to ROADMAP.md.

UPDATE 1 — Phase 4, SC-1: Replace the current SC-1 text:
  "A caller can invoke finalizeCurrentTurn({ isSubagent: true }) or finalizeCurrentTurn({}) without type errors; existing call sites with no argument also compile without change"
with:
  "Two closures exist in claudeRemoteAgentSdk.ts: finalizeCurrentTurn() (parent path, params?: { completionEvent?: string }) and finalizeSubagentTurn() (subagent path, no params). The task_notification handler calls finalizeSubagentTurn(); result handlers call finalizeCurrentTurn(). All call sites compile without errors."

Also update the Phase 4 summary line in the Phases list section from:
  "Split into unconditional bookkeeping (Phase A) and parent-only notification (Phase B); add isSubagent param; write tests first"
to:
  "Split into finalizeCurrentTurn() (parent, Phase A + B) and finalizeSubagentTurn() (subagent, Phase A only); write tests first"

UPDATE 2 — Phase 5: Revise the goal, SC-1, and SC-3 to target behavioral verification under the two-function-split design.

Replace Phase 5 goal:
  "Every call site of finalizeCurrentTurn() passes the correct isSubagent value, and the live session behavior produces exactly one ready event per parent turn completion"
with:
  "End-to-end verification that the two-function-split produces correct relay behavior: exactly one ready event per parent completion, no spurious ready events on subagent completion"

Replace Phase 5 SC-1:
  "The task_notification handler passes { isSubagent: true } and the result handler passes {} — verifiable by reading the three updated call sites in claudeRemoteAgentSdk.ts"
with:
  "The task_notification handler calls finalizeSubagentTurn() and does not call finalizeCurrentTurn() — verifiable by reading claudeRemoteAgentSdk.ts lines around the task_notification branch"

Replace Phase 5 SC-3:
  "A test scenario of parent completion with no preceding subagent also produces exactly one ready notification — the refactor does not regress the baseline path"
with:
  "A test scenario of parent completion with no preceding subagent produces exactly one ready notification — the two-function split does not regress the baseline path (TURN-06 baseline case)"

Leave SC-2 unchanged ("subagent completion followed by parent completion produces exactly one ready notification").

Update the Phase 5 Requirements line from "TURN-04, TURN-06" to "TURN-06" (TURN-04 is now satisfied by Phase 4's implementation).

Update the summary list line for Phase 5 from:
  "Wire Call Sites and Verify — Pass correct isSubagent flag at each call site; confirm exactly one ready event fires per parent-completes-after-subagent sequence"
to:
  "Verify End-to-End Behavior — Confirm exactly one ready event fires per parent turn completion; no spurious ready events on subagent completion"
  </action>
  <verify>grep -n "finalizeSubagentTurn\|isSubagent: true" /home/thamw/development/happier/happier/.planning/ROADMAP.md | head -10</verify>
  <done>ROADMAP.md Phase 4 SC-1 no longer references finalizeCurrentTurn({ isSubagent: true }). Phase 5 goal and SCs target TURN-06 behavioral verification. No remaining references to isSubagent flag being passed at call sites.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Planning artifacts | Markdown files read/written by Claude — no runtime security surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-m5g-01 | Tampering | REQUIREMENTS.md | accept | Planning doc updates only; no secrets, no executable code, no PII |
</threat_model>

<verification>
After both tasks complete:
1. grep -n "finalizeSubagentTurn" .planning/REQUIREMENTS.md — must return at least 2 hits (TURN-01 and TURN-04 descriptions)
2. grep -n "isSubagent: true" .planning/ROADMAP.md — must return 0 hits (all removed)
3. grep -n "isSubagent?: boolean" .planning/REQUIREMENTS.md — must return 0 hits (TURN-01 rewritten)
4. grep -n "TURN-06" .planning/ROADMAP.md — must appear in Phase 5 Requirements line
</verification>

<success_criteria>
- REQUIREMENTS.md TURN-01 describes finalizeSubagentTurn() as a separate closure, not an isSubagent? flag
- REQUIREMENTS.md TURN-04 describes direct finalizeSubagentTurn() call at task_notification, not flag passing
- ROADMAP.md Phase 4 SC-1 describes the actual two-function design (no "isSubagent: true" call syntax)
- ROADMAP.md Phase 5 goal targets TURN-06 behavioral verification, not call-site flag wiring
- Both files remain valid Markdown, consistent with each other and with 04-VERIFICATION.md evidence
</success_criteria>

<output>
After completion, create `.planning/quick/260419-m5g-update-requirements-md-turn-01-and-roadm/260419-m5g-SUMMARY.md` with:
- What was changed in each file and why
- The key before/after for TURN-01, TURN-04, Phase 4 SC-1, Phase 5 goal
- Confirmation that the documentation now matches the codebase implementation
</output>
