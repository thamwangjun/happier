---
phase: 11-tech-debt-cleanup
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/phases/07-server-storage-layer/07-01-SUMMARY.md
  - .planning/phases/07-server-storage-layer/07-02-SUMMARY.md
  - .planning/phases/07-server-storage-layer/07-03-SUMMARY.md
  - .planning/phases/08-server-socket-integration/08-01-SUMMARY.md
  - .planning/phases/08-server-socket-integration/08-02-SUMMARY.md
  - .planning/phases/09-mobile-reconnect-and-deduplication/09-01-SUMMARY.md
  - .planning/phases/09-mobile-reconnect-and-deduplication/09-02-SUMMARY.md
  - .planning/phases/09-mobile-reconnect-and-deduplication/09-03-SUMMARY.md
  - .planning/phases/09-mobile-reconnect-and-deduplication/09-04-SUMMARY.md
autonomous: true
requirements:
  - PROTO-01
  - PROTO-02
  - PROTO-03
  - PROTO-04
  - PROTO-05
  - STORE-01
  - STORE-02
  - STORE-03
  - STORE-04
  - STORE-05
  - STORE-06
  - STORE-07
  - SRVR-01
  - SRVR-02
  - SRVR-03
  - SRVR-04
  - SRVR-05
  - SRVR-06
  - SRVR-07
  - SRVR-08
  - SRVR-09
  - SRVR-10
  - MOB-01
  - MOB-02
  - MOB-03
  - MOB-04
  - MOB-05
  - MOB-06
  - MOB-07
  - MOB-08
  - MOB-09
  - MOB-10

must_haves:
  truths:
    - "REQUIREMENTS.md shows [x] for all 32 implementation requirements (PROTO-01 through MOB-10)"
    - "ROADMAP.md progress table shows completion dates for Phases 6-10 (no ' - ' in those rows)"
    - "Every Phase 7, 8, and 9 SUMMARY.md file has a requirements-completed frontmatter field"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Traceability table with Complete status and checked checkboxes for all 32 requirements"
      contains: "[x] **PROTO-01**"
    - path: ".planning/ROADMAP.md"
      provides: "Progress table with completion dates for Phases 6-10"
      contains: "2026-04-21"
    - path: ".planning/phases/07-server-storage-layer/07-01-SUMMARY.md"
      provides: "requirements-completed frontmatter"
      contains: "requirements-completed:"
    - path: ".planning/phases/09-mobile-reconnect-and-deduplication/09-04-SUMMARY.md"
      provides: "requirements-completed frontmatter"
      contains: "requirements-completed:"
  key_links:
    - from: "REQUIREMENTS.md checkboxes"
      to: "Traceability table statuses"
      via: "manual audit"
      pattern: "\\[x\\].*PROTO|STORE|SRVR|MOB"
---

<objective>
Update REQUIREMENTS.md checkboxes (32 pending → complete), Traceability table statuses
(Pending → Complete), ROADMAP.md progress table dates, and add `requirements-completed`
frontmatter to all Phase 7, 8, and 9 SUMMARY.md files.

Purpose: The v1.3 milestone audit found that requirement traceability records do not reflect
actual implementation state — all 32 requirements are satisfied but none are marked complete.
This plan corrects the records so the milestone can be archived accurately.

Output: REQUIREMENTS.md with all implementation requirements checked and marked Complete;
ROADMAP.md with accurate Phase 6-10 completion dates; 9 SUMMARY.md files with
requirements-completed frontmatter.
</objective>

<execution_context>
@/home/thamw/.claude/get-shit-done/workflows/execute-plan.md
@/home/thamw/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mark all 32 implementation requirements complete in REQUIREMENTS.md</name>
  <read_first>
    - .planning/REQUIREMENTS.md (current checkbox states and Traceability table)
  </read_first>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
    Update the following in REQUIREMENTS.md:

    1. Change ALL `[ ]` checkboxes to `[x]` for requirements PROTO-01 through MOB-10.
       Specifically, these 32 lines must change from `- [ ]` to `- [x]`:
       - PROTO-01, PROTO-02, PROTO-03, PROTO-04, PROTO-05 (Protocol Contract section)
       - STORE-01, STORE-02, STORE-03, STORE-04, STORE-05, STORE-06, STORE-07 (Server Storage Layer section)
       - SRVR-01, SRVR-02, SRVR-03, SRVR-04, SRVR-05, SRVR-06, SRVR-07, SRVR-08, SRVR-09, SRVR-10 (Server Socket Integration section)
       - MOB-01, MOB-02, MOB-03, MOB-04, MOB-05, MOB-06, MOB-07, MOB-08, MOB-09, MOB-10 (Mobile/Web section)

       VALID-01 through VALID-04 are already `[x]` — leave them unchanged.

    2. In the Traceability table (at the bottom of the file), change all `Pending` statuses
       to `Complete` for rows PROTO-01 through MOB-10 (30 rows total).
       VALID-01 through VALID-04 already show `Complete` — leave them unchanged.

       Each row changes from:
         `| PROTO-01 | Phase 6 | Pending |`
       to:
         `| PROTO-01 | Phase 6 | Complete |`
  </action>
  <verify>
    <automated>grep -c '\- \[ \]' .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '\- \[ \]' .planning/REQUIREMENTS.md` outputs `0` (no unchecked implementation requirements remain)
    - `grep -c '\- \[x\] \*\*PROTO' .planning/REQUIREMENTS.md` outputs `5`
    - `grep -c '\- \[x\] \*\*STORE' .planning/REQUIREMENTS.md` outputs `7`
    - `grep -c '\- \[x\] \*\*SRVR' .planning/REQUIREMENTS.md` outputs `10`
    - `grep -c '\- \[x\] \*\*MOB' .planning/REQUIREMENTS.md` outputs `10`
    - `grep -c '| Pending |' .planning/REQUIREMENTS.md` outputs `0`
    - `grep -c '| Complete |' .planning/REQUIREMENTS.md` outputs `34` (30 updated + 4 already complete)
  </acceptance_criteria>
  <done>All 32 implementation requirements show [x] and Complete in REQUIREMENTS.md. Zero Pending rows remain in the Traceability table.</done>
</task>

<task type="auto">
  <name>Task 2: Fix ROADMAP.md progress table dates and phase checkboxes</name>
  <read_first>
    - .planning/ROADMAP.md (current progress table, v1.3 phase list)
  </read_first>
  <files>.planning/ROADMAP.md</files>
  <action>
    Two updates to ROADMAP.md:

    1. In the Progress table (the markdown table near the bottom), update the `Completed`
       column for Phases 6-10. The current values for Phases 6-9 show ` - `. Change them:
       - Phase 6 row: change ` - ` to `2026-04-21`
       - Phase 7 row: change ` - ` to `2026-04-22`
       - Phase 8 row: change ` - ` to `2026-04-22`
       - Phase 9 row: change ` - ` to `2026-04-23`
       - Phase 10 row: already shows `2026-04-23` — leave unchanged.
       - Phase 11 row: leave as ` - ` (not yet complete).

    2. In the `### v1.3 Request Resilience` section (around line 29), ensure the Phase 11
       bullet shows `[ ]` (it is not yet complete). Phases 6-10 should already show `[x]`
       with their completion dates in parentheses based on current ROADMAP.md content —
       verify they do; if any are missing `[x]`, add it.
  </action>
  <verify>
    <automated>grep -A 16 '| Phase | Milestone |' .planning/ROADMAP.md | grep -E 'Phase [6-9]\.'</automated>
  </verify>
  <acceptance_criteria>
    - `grep '6. Protocol Contract' .planning/ROADMAP.md` contains `2026-04-21` in the same line
    - `grep '7. Server Storage Layer' .planning/ROADMAP.md` contains `2026-04-22` in the same line
    - `grep '8. Server Socket Integration' .planning/ROADMAP.md` contains `2026-04-22` in the same line
    - `grep '9. Mobile Reconnect' .planning/ROADMAP.md` contains `2026-04-23` in the same line
    - `grep '11. Tech Debt Cleanup' .planning/ROADMAP.md` does NOT contain a completion date (row ends with `| - |`)
  </acceptance_criteria>
  <done>ROADMAP.md progress table shows accurate completion dates for all completed phases. Phase 11 remains undated.</done>
</task>

<task type="auto">
  <name>Task 3: Add requirements-completed frontmatter to Phase 7, 8, 9 SUMMARY.md files</name>
  <read_first>
    - .planning/phases/07-server-storage-layer/07-01-SUMMARY.md (existing frontmatter, closing --- delimiter)
    - .planning/phases/07-server-storage-layer/07-02-SUMMARY.md (existing frontmatter)
    - .planning/phases/07-server-storage-layer/07-03-SUMMARY.md (existing frontmatter)
    - .planning/phases/08-server-socket-integration/08-01-SUMMARY.md (existing frontmatter)
    - .planning/phases/08-server-socket-integration/08-02-SUMMARY.md (existing frontmatter)
    - .planning/phases/09-mobile-reconnect-and-deduplication/09-01-SUMMARY.md (existing frontmatter)
    - .planning/phases/09-mobile-reconnect-and-deduplication/09-02-SUMMARY.md (existing frontmatter)
    - .planning/phases/09-mobile-reconnect-and-deduplication/09-03-SUMMARY.md (existing frontmatter)
    - .planning/phases/09-mobile-reconnect-and-deduplication/09-04-SUMMARY.md (existing frontmatter)
  </read_first>
  <files>
    .planning/phases/07-server-storage-layer/07-01-SUMMARY.md,
    .planning/phases/07-server-storage-layer/07-02-SUMMARY.md,
    .planning/phases/07-server-storage-layer/07-03-SUMMARY.md,
    .planning/phases/08-server-socket-integration/08-01-SUMMARY.md,
    .planning/phases/08-server-socket-integration/08-02-SUMMARY.md,
    .planning/phases/09-mobile-reconnect-and-deduplication/09-01-SUMMARY.md,
    .planning/phases/09-mobile-reconnect-and-deduplication/09-02-SUMMARY.md,
    .planning/phases/09-mobile-reconnect-and-deduplication/09-03-SUMMARY.md,
    .planning/phases/09-mobile-reconnect-and-deduplication/09-04-SUMMARY.md
  </files>
  <action>
    Add a `requirements-completed` YAML list field to each SUMMARY.md's frontmatter block.
    Insert the field as the LAST entry before the closing `---` delimiter in each file's
    frontmatter. Do NOT modify any content after the frontmatter.

    Exact field values per file:

    07-01-SUMMARY.md — add before closing `---`:
    ```yaml
    requirements-completed:
      - STORE-01
      - STORE-02
      - STORE-04
      - STORE-05
      - STORE-07
    ```

    07-02-SUMMARY.md — add before closing `---`:
    ```yaml
    requirements-completed:
      - STORE-01
      - STORE-02
      - STORE-04
      - STORE-05
      - STORE-07
    ```

    07-03-SUMMARY.md — add before closing `---`:
    ```yaml
    requirements-completed:
      - STORE-03
      - STORE-06
    ```

    08-01-SUMMARY.md — add before closing `---`:
    ```yaml
    requirements-completed:
      - SRVR-01
      - SRVR-02
      - SRVR-03
      - SRVR-04
      - SRVR-05
      - SRVR-06
      - SRVR-07
      - SRVR-08
      - SRVR-09
      - SRVR-10
    ```

    08-02-SUMMARY.md — add before closing `---`:
    ```yaml
    requirements-completed:
      - SRVR-01
      - SRVR-02
      - SRVR-03
      - SRVR-04
      - SRVR-05
      - SRVR-06
      - SRVR-07
      - SRVR-08
      - SRVR-09
      - SRVR-10
    ```

    09-01-SUMMARY.md — add before closing `---`:
    ```yaml
    requirements-completed:
      - MOB-01
      - MOB-02
      - MOB-03
      - MOB-04
      - MOB-05
      - MOB-06
      - MOB-07
      - MOB-08
      - MOB-09
      - MOB-10
    ```

    09-02-SUMMARY.md — add before closing `---`:
    ```yaml
    requirements-completed:
      - MOB-04
    ```

    09-03-SUMMARY.md — add before closing `---`:
    ```yaml
    requirements-completed:
      - MOB-01
      - MOB-02
      - MOB-03
      - MOB-05
      - MOB-06
      - MOB-07
      - MOB-08
      - MOB-09
      - MOB-10
    ```

    09-04-SUMMARY.md — add before closing `---`:
    ```yaml
    requirements-completed:
      - MOB-02
      - MOB-03
    ```
  </action>
  <verify>
    <automated>grep -l 'requirements-completed:' .planning/phases/07-server-storage-layer/*-SUMMARY.md .planning/phases/08-server-socket-integration/*-SUMMARY.md .planning/phases/09-mobile-reconnect-and-deduplication/*-SUMMARY.md | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - The command above outputs `9` (all 9 SUMMARY.md files contain the field)
    - `grep 'requirements-completed:' .planning/phases/07-server-storage-layer/07-01-SUMMARY.md` finds exactly one match
    - `grep 'requirements-completed:' .planning/phases/07-server-storage-layer/07-03-SUMMARY.md` finds exactly one match
    - `grep -A 2 'requirements-completed:' .planning/phases/07-03-SUMMARY.md 2>/dev/null || grep -A 2 'requirements-completed:' .planning/phases/07-server-storage-layer/07-03-SUMMARY.md` shows `- STORE-03` and `- STORE-06`
    - `grep -A 1 'requirements-completed:' .planning/phases/09-mobile-reconnect-and-deduplication/09-02-SUMMARY.md` shows `- MOB-04`
    - Each file's frontmatter still opens with `---` and the body content after the closing `---` is unchanged
  </acceptance_criteria>
  <done>All 9 SUMMARY.md files in Phases 7, 8, and 9 have a requirements-completed frontmatter field listing the REQ-IDs each plan satisfied. Frontmatter structure is valid YAML.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| file writes | All changes are to .planning/ markdown files — no executable code, no network calls |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01-01 | Tampering | REQUIREMENTS.md edit | accept | Changes are auditable via git diff; no code execution involved |
| T-11-01-02 | Repudiation | SUMMARY.md frontmatter | accept | All edits committed to git with standard commit message — full audit trail |
</threat_model>

<verification>
After all three tasks complete:

```bash
# Confirm zero unchecked implementation requirements
grep -c '\- \[ \]' .planning/REQUIREMENTS.md
# Expected: 0

# Confirm all 32 implementation requirements are checked
grep -c '\- \[x\]' .planning/REQUIREMENTS.md
# Expected: 36 (32 implementation + 4 VALID already done)

# Confirm Phase 6-9 dates appear in ROADMAP.md
grep -E '2026-04-2[123]' .planning/ROADMAP.md | head -10

# Confirm all 9 SUMMARY.md files have requirements-completed
grep -rl 'requirements-completed:' .planning/phases/0[789]-*/
# Expected: 9 files
```
</verification>

<success_criteria>
- REQUIREMENTS.md: `grep -c '\- \[ \]'` returns 0
- REQUIREMENTS.md: `grep -c '| Pending |'` returns 0
- ROADMAP.md: Phase 6 progress row contains `2026-04-21`
- ROADMAP.md: Phase 7 progress row contains `2026-04-22`
- ROADMAP.md: Phase 8 progress row contains `2026-04-22`
- ROADMAP.md: Phase 9 progress row contains `2026-04-23`
- All 9 Phase 7/8/9 SUMMARY.md files contain `requirements-completed:` in frontmatter
</success_criteria>

<output>
After completion, create `.planning/phases/11-tech-debt-cleanup/11-01-SUMMARY.md`
</output>
