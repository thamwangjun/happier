---
phase: 11-tech-debt-cleanup
plan: "03"
type: execute
wave: 2
depends_on:
  - "11-01"
files_modified:
  - docs/protocol.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - SRVR-09
  - SRVR-10

must_haves:
  truths:
    - "docs/protocol.md replay-complete section documents { retentionStart: number } payload instead of 'No payload.'"
    - "REQUIREMENTS.md VALID-03 description names the correct test (buffer.walContention.stress.test.ts, PostgreSQL connection pool, not SQLite WAL contention)"
  artifacts:
    - path: "docs/protocol.md"
      provides: "Accurate replay-complete payload documentation"
      contains: "retentionStart: number"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Accurate VALID-03 requirement description"
      contains: "buffer.walContention.stress.test.ts"
  key_links:
    - from: "docs/protocol.md replay-complete entry"
      to: "resilienceHandler.ts actual emit payload"
      via: "manual audit of server emit call"
      pattern: "retentionStart"
---

<objective>
Update docs/protocol.md to document the actual `{ retentionStart: number }` payload emitted
by the server's `replay-complete` event, and fix the VALID-03 requirement description in
REQUIREMENTS.md to accurately name the test and the database contention being validated.

Purpose: The v1.3 audit identified two description mismatches. The `replay-complete` entry
in protocol.md says "No payload." but the server actually emits `{ retentionStart }`. The
VALID-03 description references "SQLite WAL contention" but the load test targets PostgreSQL
connection pool saturation. These are documentation bugs, not implementation bugs.

Output: protocol.md with a correct replay-complete payload block; REQUIREMENTS.md with an
accurate VALID-03 description.

Note: depends_on 11-01 because 11-01 also edits REQUIREMENTS.md (checkbox + traceability
table updates). This plan's VALID-03 change must be applied to the already-updated file.
</objective>

<execution_context>
@/home/thamw/.claude/get-shit-done/workflows/execute-plan.md
@/home/thamw/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update docs/protocol.md replay-complete and buffer-overflow payload documentation</name>
  <read_first>
    - docs/protocol.md (read the full "Server → client events" section, especially replay-complete and buffer-overflow entries)
    - apps/server/sources/app/api/socket/resilienceHandler.ts (find the socket.emit("replay-complete", ...) and socket.emit("buffer-overflow", ...) calls to confirm actual payload shapes)
  </read_first>
  <files>docs/protocol.md</files>
  <action>
    1. Find the `#### \`replay-complete\`` section. It currently contains:
       ```
       No payload.
       ```
       Replace the entire section body with:
       ```markdown
       #### `replay-complete`
       Emitted by the server in all reconnect paths: after the last buffered message is sent,
       after `buffer-overflow` is signalled, or immediately if the buffer is empty.
       This is the universal gate-release signal for the mobile outbound queue.

       Payload: `{ retentionStart: number | null }` — the oldest `seq` still in the buffer at the
       time of replay. `null` when the buffer is empty. Mobile clients use this to detect
       non-contiguous gaps (see MOB-09).
       ```

       Note on the type: the server emits `{ retentionStart: null }` when the buffer is empty
       (confirmed by the integration test at line 113 of resilienceHandler.integration.spec.ts:
       `expect(socket.emit).toHaveBeenCalledWith("replay-complete", { retentionStart: null })`).
       So the correct type is `number | null`, not `number`.

    2. Find the `#### \`buffer-overflow\`` section. Read resilienceHandler.ts to confirm
       whether buffer-overflow is emitted with or without a payload. If it is emitted as
       `socket.emit("buffer-overflow")` with no second argument, the "No payload." text is
       accurate — leave it unchanged. If it has a payload, update similarly.

       Based on the integration spec at line 130 (`expect(socket.emit).toHaveBeenCalledWith("buffer-overflow")`
       — single argument, no payload object), buffer-overflow has no payload. Leave that
       section unchanged.
  </action>
  <verify>
    <automated>grep -A 5 '#### `replay-complete`' docs/protocol.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'No payload\.' docs/protocol.md` returns 0 matches for the replay-complete section (the buffer-overflow "No payload." may still exist)
    - `grep 'retentionStart: number' docs/protocol.md` returns at least 1 match
    - `grep 'retentionStart: number | null' docs/protocol.md` returns at least 1 match (the type is nullable)
    - `grep -A 8 '#### .replay-complete.' docs/protocol.md` contains `retentionStart` within 8 lines of the heading
    - The `#### \`replay-complete\`` heading still exists (section not deleted)
    - `grep -c '#### `replay-complete`' docs/protocol.md` returns `1` (exactly one heading)
  </acceptance_criteria>
  <done>
    docs/protocol.md replay-complete section documents `{ retentionStart: number | null }` payload.
    The buffer-overflow section is verified and left unchanged (no payload).
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix VALID-03 requirement description in REQUIREMENTS.md</name>
  <read_first>
    - .planning/REQUIREMENTS.md (read the VALID-03 line and surrounding context — note: 11-01 has already updated checkboxes and traceability, so this is editing the updated file)
  </read_first>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
    Find the VALID-03 requirement line. Current text:
    ```
    - [x] **VALID-03**: Load test validates SQLite WAL contention under high-frequency transcript streaming with the `UnackedMessageBuffer` active
    ```

    Replace with:
    ```
    - [x] **VALID-03**: Load test validates connection pool saturation under high-frequency transcript streaming — 200 concurrent `writeToBuffer` calls via `Promise.all` (`buffer.walContention.stress.test.ts`)
    ```

    The `[x]` checkbox state must remain unchanged (it was already [x] before this phase).
    Only the description text after the `**VALID-03**:` changes.
  </action>
  <verify>
    <automated>grep 'VALID-03' .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'VALID-03' .planning/REQUIREMENTS.md` output contains `buffer.walContention.stress.test.ts`
    - `grep 'VALID-03' .planning/REQUIREMENTS.md` output does NOT contain `SQLite WAL contention`
    - `grep 'VALID-03' .planning/REQUIREMENTS.md` output begins with `- [x] **VALID-03**` (checkbox preserved)
    - `grep -c 'VALID-03' .planning/REQUIREMENTS.md` returns `2` (one in requirements list, one in traceability table — both unchanged by this edit except the description line)
  </acceptance_criteria>
  <done>
    VALID-03 requirement description accurately names the test file (buffer.walContention.stress.test.ts)
    and the contention type (connection pool saturation, not SQLite WAL contention).
    The [x] checkbox is preserved.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs edit | Modifying markdown documentation files only — no code, no secrets, no network calls |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03-01 | Tampering | docs/protocol.md | accept | Change is purely documentary; git history provides full audit trail; no executable surface |
| T-11-03-02 | Information Disclosure | retentionStart payload in docs | accept | retentionStart is a sequence counter (non-sensitive integer); documenting it does not expose PII or secrets |
</threat_model>

<verification>
```bash
# Confirm replay-complete payload is documented
grep -A 6 '#### `replay-complete`' docs/protocol.md
# Must contain: retentionStart: number | null

# Confirm VALID-03 description is updated
grep 'VALID-03' .planning/REQUIREMENTS.md
# Must contain: buffer.walContention.stress.test.ts
# Must NOT contain: SQLite WAL contention
```
</verification>

<success_criteria>
- `grep 'No payload\.' docs/protocol.md` returns 0 results in the replay-complete section
- `grep 'retentionStart: number | null' docs/protocol.md` returns 1 result
- `grep 'VALID-03' .planning/REQUIREMENTS.md` contains `buffer.walContention.stress.test.ts`
- `grep 'SQLite WAL contention' .planning/REQUIREMENTS.md` returns 0 results
</success_criteria>

<output>
After completion, create `.planning/phases/11-tech-debt-cleanup/11-03-SUMMARY.md`
</output>
